const SECOND_MS = 1000;
const MINUTE_MS = 60 * SECOND_MS;
const HOUR_MS = 60 * MINUTE_MS;
const DAY_MS = 24 * HOUR_MS;

export const CACHE_TTL_MS = {
  DEFAULT: DAY_MS,
  OAUTH_STATE: 5 * MINUTE_MS,
  OAUTH_EXCHANGE_CODE: 5 * MINUTE_MS,
  INTEGRATION_CONNECT_CODE: 5 * MINUTE_MS,
  INTEGRATION_IDEMPOTENCY: 10 * MINUTE_MS,
  INTEGRATION_RATE_LIMIT_WINDOW: MINUTE_MS,
  CSRF_TOKEN: DAY_MS,
  PRE_AUTH_CSRF_TOKEN: DAY_MS,
  PASSWORD_RESET_TOKEN: 15 * MINUTE_MS,
  AUTH_RATE_LIMIT_WINDOW: 15 * MINUTE_MS,
  AUTH_RATE_LIMIT_METRICS: DAY_MS,
} as const;
